<% if @user.errors.any? %>
  <div id="error_explanation">
    <h3>There were <%= pluralize(@user.errors.count, "error") %>:</h3>
    <ul>
    <% @user.errors.full_messages.each do |m| %>
      <li><%= m %></li>
    <% end %>
    </ul>
  </div>
<% end %>